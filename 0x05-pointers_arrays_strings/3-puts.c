#include "main.h"

/**
 * _puts - entry point for a string followed by a new line
 * @str: the string
 *
 * Return: void
 */

void _puts(char *str)
{
	int i;

	for (i = 0; i < 1; i++)
	{
		printf("%s\n", str);
	}
}
