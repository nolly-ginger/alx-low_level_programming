#include "main.h"

/**
 * main - used to test whether integer is positive or negative
 * gives corrupt for value=0
 *
 * Return: Always 0(Success)
 */

int main(void)
{
	int i;
	int != 0;

	i = 45;
	positive_or_negative(i);

	return (0);
}
