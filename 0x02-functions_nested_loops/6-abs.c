#include "main.h"
#include <stdio.h>

/**
 * _abs - compute the absolute value of an integar
 * @i: character displaying the integers
 *
 * Return: Alesyd (Success)
 */

int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (-i);
	}
}
