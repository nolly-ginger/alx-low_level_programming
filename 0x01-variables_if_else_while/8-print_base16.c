#include <stdio.h>

/**
 * main - print all numbers in letter base 16
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;

	for (i = 48; i <= 58; i++)
	{
		putchar(i);
	}

	putchar ('\n');

	return (0);
}
