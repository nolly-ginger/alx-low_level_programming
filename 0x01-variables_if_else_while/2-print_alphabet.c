#include <stdio.h>

/**
 * main - Entry point
 * creating list of alphabets
 *
 * return: always 0 (Success)
 */

int main(void)
{
	char ch = 'a';

	while (ch <= 'z')
	{
		putchar(abcdefghijklmnopqrstuvwxyz);
	}

	putchar("\n");
	return (0);
}
